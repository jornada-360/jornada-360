import{describe,it,expect}from'vitest';import{LegalValidationEngine}from'../src/legal/engine.js';import type{LegalContext}from'../src/legal/types.js';
const make=(count:number):LegalContext=>({weekStart:'2026-08-31',weekEnd:'2026-09-06',history:[],openingHours:[],employeeMasterAvailable:false,rules:[{id:'days',code:'MAX_CONSECUTIVE_WORK_DAYS',name:'Máximo días consecutivos',category:'HISTORICAL',valid_from:'2026-04-26',severity:'ERROR',scope:'GLOBAL',parameter_type:'INTEGER',parameter_value:'6',enabled:1,blocking:1,version:1}],employees:[{rowId:'row-excel',storeName:'Local',employee:{rut:'123456785',name:'Ejemplo'},days:Array.from({length:7},(_,i)=>({id:String(i),date:`2026-0${i<1?'8-31':`9-0${i}`}`,dayName:'LUNES',dayType:i<count?'WORK':'FREE',intervals:i<count?[{type:'WORK',start:'09:00',end:'17:00',position:1}]:[]}))as any}]});
describe('máximo seis días consecutivos sin maestro',()=>{it('acepta seis',()=>expect(new LegalValidationEngine().run(make(6)).results.some(x=>x.ruleCode==='MAX_CONSECUTIVE_WORK_DAYS')).toBe(false));it('bloquea el séptimo',()=>{const result=new LegalValidationEngine().run(make(7));expect(result.results.find(x=>x.ruleCode==='MAX_CONSECUTIVE_WORK_DAYS')).toMatchObject({blocking:true,actualValue:'7',expectedValue:'6'});expect(result.blockingErrors).toBe(1)})});
