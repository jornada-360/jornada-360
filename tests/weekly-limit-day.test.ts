import{it,expect}from'vitest';import{LegalValidationEngine}from'../src/legal/engine.js';import type{LegalContext}from'../src/legal/types.js';
it('identifica el turno que hace superar las 42 horas',()=>{const context:LegalContext={weekStart:'2026-10-05',weekEnd:'2026-10-11',history:[],openingHours:[],rules:[{id:'weekly',code:'MAX_WEEKLY_ORDINARY_HOURS',name:'Máximo semanal',category:'LABOR_LEGAL',valid_from:'2026-04-26',severity:'ERROR',scope:'GLOBAL',parameter_type:'MINUTES',parameter_value:'2520',enabled:1,blocking:1,version:1}],employees:[{rowId:'row',storeName:'Local',employee:{rut:'1',name:'Vendedor'},days:Array.from({length:6},(_,i)=>({id:String(i),date:`2026-10-${String(i+5).padStart(2,'0')}`,dayName:'LUNES',dayType:'WORK',intervals:[{type:'WORK',start:'09:00',end:'17:00',position:1}]}))as any}]};const result=new LegalValidationEngine().run(context).results.find(x=>x.ruleCode==='MAX_WEEKLY_ORDINARY_HOURS');expect(result?.date).toBe('2026-10-10');expect(result?.metadata).toMatchObject({rowId:'row',accumulatedAtViolation:2880,excessMinutes:360,causingDayMinutes:480})});
