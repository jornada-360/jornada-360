import {describe,it,expect} from 'vitest';import {parseDay,validRut,formatDay} from '../src/domain.js';
describe('parser de días',()=>{it.each([['11:15 a 19:45','WORK',1],['11:15 a 19:45\nCol 15:00 a 16:00','WORK',2],['09:00 a 13:00\n17:00 a 21:00','WORK',2],['L','FREE',0],['l','FREE',0],['Vacaciones','VACATION',0]])('reconoce %s',(raw,type,n)=>{const x=parseDay(raw);expect(x.dayType).toBe(type);expect(x.intervals).toHaveLength(n)});it('marca celda vacía',()=>expect(parseDay('').issues[0].code).toBe('EMPTY_DAY'));it('rechaza formato inválido',()=>expect(parseDay('11.15 hasta 8').dayType).toBe('INVALID'));it('rechaza colación fuera del turno',()=>expect(parseDay('09:00 a 13:00\nCol 14:00 a 15:00').issues.some(x=>x.code==='BREAK_OUTSIDE_WORK')).toBe(true));it('rechaza turnos superpuestos',()=>expect(parseDay('09:00 a 14:00\n13:00 a 17:00').issues.some(x=>x.code==='OVERLAPPING_WORK_INTERVALS')).toBe(true));it('normaliza al exportar',()=>expect(formatDay('VACATION',[])).toBe('Vac'))});
describe('RUT chileno',()=>{it('valida DV',()=>{expect(validRut('12.345.678-5')).toBe(true);expect(validRut('12.345.678-9')).toBe(false)})});
describe('estados informativos sin jornada',()=>{it.each([['Ausencia','ABSENCE'],['Feriado irrenunciable','UNWAIVABLE_HOLIDAY'],['DA','ADDITIONAL_SUNDAY']])('acepta %s sin error de formato',(value,type)=>{const parsed=parseDay(value);expect(parsed.dayType).toBe(type);expect(parsed.issues).toEqual([]);expect(parsed.intervals).toEqual([])})});
